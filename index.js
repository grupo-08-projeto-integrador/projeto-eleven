function contador(){
    var contmais=document.getElementsByClassName("mais")
    var contmenos=document.getElementsByClassName("menos")
    if(contmais != null)
        var cont=Number(cont+1);
    if(contmenos != null)
        var cont=Number(cont-1);
    console.log(cont)
}